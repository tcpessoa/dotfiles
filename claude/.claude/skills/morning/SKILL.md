---
name: morning
description: (deprecated alias) Old name for /next — the orient-and-pick loop. Behavior unchanged. Scheduled for removal ~2026-06-29.
---

`/morning` was renamed to **`/next`** (see vault `adr/0004` — loops are named for function, not time of day).

Invoke the `next` skill now (Skill tool, `skill: "next"`) and follow it exactly — behavior is unchanged. This alias exists only for muscle memory and will be removed around 2026-06-29.
