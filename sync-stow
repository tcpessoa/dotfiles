#!/bin/bash
packages=(
  alacritty
  btop
  ghostty
  git
  lazygit
  starship
  taskwarrior
  tms
  tmux
  zsh
  # scripts
)

for package in "${packages[@]}"; do
  stow -R -v -t ~ -d ~/dotfiles "$package"
done
